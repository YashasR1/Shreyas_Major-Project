{
  "name": "smart-ration-platform",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm --workspace=apps/kiosk-app run build",
    "build:kiosk": "npm --workspace=apps/kiosk-app run build",
    "dev:mobile": "npm --workspace=apps/mobile-app run dev",
    "dev:kiosk": "npm --workspace=apps/kiosk-app run dev",
    "test:mobile": "npm --workspace=apps/mobile-app run test"
  }
}
